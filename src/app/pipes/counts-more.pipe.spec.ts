import { CountsMorePipe } from './counts-more.pipe';

describe('CountsMorePipe', () => {
  it('create an instance', () => {
    const pipe = new CountsMorePipe();
    expect(pipe).toBeTruthy();
  });
});
